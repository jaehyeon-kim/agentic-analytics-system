wrenai[trino,memory,main,mcp]
strands-agents
strands-agents-tools
litellm[proxy]
boto3
mem0ai[nlp]
fastembed
valkey>=6.0.0
redisvl
opentelemetry-api
python-dotenv
# Pinned, not a floor, so a later dynamic-des cannot change generated data under
# this project without the pin being lifted deliberately.
dynamic-des[parquet]==0.12.0
pyiceberg[s3fs]
odctl
